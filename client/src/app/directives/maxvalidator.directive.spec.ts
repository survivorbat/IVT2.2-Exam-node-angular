import { MaxvalidatorDirective } from './maxvalidator.directive';

describe('MaxvalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxvalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
