import { MinvalidatorDirective } from './minvalidator.directive';

describe('MinvalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MinvalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
